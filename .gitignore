# Dependencies
node_modules/
# npm is the lockfile source of truth — do not reintroduce Bun lockfiles (Trivy SCA noise)
bun.lock
bun.lockb
yarn.lock
pnpm-lock.yaml

# Build
dist/
coverage/
*.tsbuildinfo

# Environment & secrets
.env
.env.local
.env.*.local
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / IDE
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo
*.sublime-*

# Prisma
prisma/*.db
prisma/*.db-journal

# Test artifacts
coverage/
.nyc_output/

# Misc local notes (keep out of the template repo)
TECHNICAL_ANALYSIS.md
ISSUES_QUICK_REFERENCE.md
OAUTH_README.md

# Optional local Docker overrides
docker-compose.override.yml

# JWT RSA material — generate with `npm run keys:generate`
keys/*.pem
keys/*.key
!keys/.gitkeep
