/**
 * Prometheus metrics router — re-exports shared metrics infrastructure.
 */
export { default as createMetricsRouter } from '@/shared/infrastructure/metrics/metrics';
export { default as metricsRouter } from '@/shared/infrastructure/metrics/metrics';
